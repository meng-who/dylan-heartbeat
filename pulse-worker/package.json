{
  "name": "dylan-pulse-worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "A fail-open body-state proxy for Kelivo and Dylan Heartbeat.",
  "scripts": {
    "test": "node --test",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:local": "wrangler d1 migrations apply pulse-state --local",
    "db:remote": "wrangler d1 migrations apply pulse-state --remote"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  },
  "engines": {
    "node": ">=20"
  }
}
